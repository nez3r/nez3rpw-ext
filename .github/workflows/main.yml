name: Builds

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  # CMake builds for both architectures
  cmake-build-arm64:
    name: CMake build (arm64-v8a)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Setup Android NDK
        uses: nttld/setup-ndk@v1.6.0
        with:
          ndk-version: r27d
        id: setup-ndk
      - name: Configure CMake (arm64-v8a)
        run: |
          cmake -S . -B build-cmake-arm64 -G Ninja \
            -DCMAKE_TOOLCHAIN_FILE="${{ steps.setup-ndk.outputs.ndk-path }}/build/cmake/android.toolchain.cmake" \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-23 \
            -DANDROID_STL=c++_static \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
      - name: Build with CMake
        run: cmake --build build-cmake-arm64 -j$(nproc)
      - uses: actions/upload-artifact@v7
        with:
          name: cmake-arm64-v8a
          path: build-cmake-arm64/libnez3r.so

  cmake-build-x86_64:
    name: CMake build (x86_64)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Setup Android NDK
        uses: nttld/setup-ndk@v1.6.0
        with:
          ndk-version: r27d
        id: setup-ndk
      - name: Configure CMake (x86_64)
        run: |
          cmake -S . -B build-cmake-x86_64 -G Ninja \
            -DCMAKE_TOOLCHAIN_FILE="${{ steps.setup-ndk.outputs.ndk-path }}/build/cmake/android.toolchain.cmake" \
            -DANDROID_ABI=x86_64 \
            -DANDROID_PLATFORM=android-23 \
            -DANDROID_STL=c++_static \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
      - name: Build with CMake
        run: cmake --build build-cmake-x86_64 -j$(nproc)
      - uses: actions/upload-artifact@v7
        with:
          name: cmake-x86_64
          path: build-cmake-x86_64/libnez3r.so

  # ndk-build for both architectures
  ndk-build-arm64:
    name: ndk-build (arm64-v8a)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Setup Android NDK
        uses: nttld/setup-ndk@v1.6.0
        with:
          ndk-version: r27d
        id: setup-ndk
      - name: Run ndk-build (arm64-v8a)
        run: |
          ndk-build \
            NDK_PROJECT_PATH=. \
            APP_BUILD_SCRIPT=./jni/Android.mk \
            APP_ABI=arm64-v8a \
            -j$(nproc)
      - uses: actions/upload-artifact@v7
        with:
          name: ndk-arm64-v8a
          path: libs/arm64-v8a/nez3r.sh

  ndk-build-x86_64:
    name: ndk-build (x86_64)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Setup Android NDK
        uses: nttld/setup-ndk@v1.6.0
        with:
          ndk-version: r27d
        id: setup-ndk
      - name: Run ndk-build (x86_64)
        run: |
          ndk-build \
            NDK_PROJECT_PATH=. \
            APP_BUILD_SCRIPT=./jni/Android.mk \
            APP_ABI=x86_64 \
            -j$(nproc)
      - uses: actions/upload-artifact@v7
        with:
          name: ndk-x86_64
          path: libs/x86_64/nez3r.sh
